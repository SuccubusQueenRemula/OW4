if (!haltLoad)
{
	//mPopulateWorldChunk(loadingWorldChunk, x, y, chunkInfo, chunkFile);
}
else
{
	mClientLogAddEntry("Populate chunk operation cancelled. Chunk loader was already destroyed.");
}
