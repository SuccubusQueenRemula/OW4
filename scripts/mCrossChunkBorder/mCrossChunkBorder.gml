///@description Sets a chunk border up to have the info it needs to handle the border crossing, then executes the crossing.
///@arg worldGrid - The ds_grid that contains the world chunks, usually belongs to oWorldHandler.
mConceiveBorderCrossing(argument0);
event_user(0);