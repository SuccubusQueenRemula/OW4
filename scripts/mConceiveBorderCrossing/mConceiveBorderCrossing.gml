///@description To be called by world chunk borders. Takes information from above and plugs it into this object so it can do a user event for border crossing.


worldGrid = argument0;

worldGridW = ds_grid_width(worldGrid);
worldGridH = ds_grid_height(worldGrid);